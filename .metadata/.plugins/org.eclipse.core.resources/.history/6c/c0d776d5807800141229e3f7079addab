package com.example.test;

import java.util.Calendar;


import android.app.Activity;
import android.content.Intent;
import android.graphics.drawable.Drawable;
import android.os.Bundle;
import android.util.Log;
import android.view.Menu;
import android.view.MenuItem;
import android.widget.LinearLayout;
import android.widget.RelativeLayout;
import android.widget.Toast;
import android.util.Log;
public class MainActivity extends Activity {
	String tag = "test"
;    @SuppressWarnings("deprecation")
	@Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.one);
        LinearLayout linearLayout = (LinearLayout) findViewById(R.id.linearlayout);
        linearLayout.setBackgroundDrawable( getResources().getDrawable(R.drawable.radar1) );
        int count = 0;
        
      while (count < 100){count++;}
        linearLayout.setBackgroundDrawable( getResources().getDrawable(R.drawable.radar2) );
         count = 0;
      while (count < 100){count++;}
        linearLayout.setBackgroundDrawable( getResources().getDrawable(R.drawable.radar3) );
        linearLayout.setBackgroundDrawable( getResources().getDrawable(R.drawable.radar4) );
        linearLayout.setBackgroundDrawable( getResources().getDrawable(R.drawable.radar5) );
        linearLayout.setBackgroundDrawable( getResources().getDrawable(R.drawable.radar6) );
        linearLayout.setBackgroundDrawable( getResources().getDrawable(R.drawable.radar7) );
        linearLayout.setBackgroundDrawable( getResources().getDrawable(R.drawable.radar8) );
        linearLayout.setBackgroundDrawable( getResources().getDrawable(R.drawable.radar9) );
        linearLayout.setBackgroundDrawable( getResources().getDrawable(R.drawable.radar10) );
        linearLayout.setBackgroundDrawable( getResources().getDrawable(R.drawable.radar11) );
        linearLayout.setBackgroundDrawable( getResources().getDrawable(R.drawable.radar12) );

//       Intent intent  = new Intent(MainActivity.this,Two.class);
//      int count = 0;
//      while (count < 100){count++;}
////       startActivity(intent);
//      
//       setContentView(R.layout.two);
//       setContentView(R.layout.three);
//        setContentView(R.layout.four);
//        setContentView(R.layout.five);
//        setContentView(R.layout.six);
//        setContentView(R.layout.seven);
//        setContentView(R.layout.eight);
//        setContentView(R.layout.nine);
//        setContentView(R.layout.ten);
//        setContentView(R.layout.eleven);
//        setContentView(R.layout.twelve);
    
    }

 @Override
    public boolean onCreateOptionsMenu(Menu menu) {
        // Inflate the menu; this adds items to the action bar if it is present.
        getMenuInflater().inflate(R.menu.main, menu);
        return true;
    }

    @Override
    public boolean onOptionsItemSelected(MenuItem item) {
        // Handle action bar item clicks here. The action bar will
        // automatically handle clicks on the Home/Up button, so long
        // as you specify a parent activity in AndroidManifest.xml.
        int id = item.getItemId();
        if (id == R.id.action_settings) {
            return true;
        }
        return super.onOptionsItemSelected(item);
    }
}
